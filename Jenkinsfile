pipeline {
  agent any
  stages {
    stage('first-step') {
      steps {
        echo 'first gogo!'
      }
    }
    stage('second-step') {
      steps {
        echo 'second-step gogo!'
      }
    }
  }
}